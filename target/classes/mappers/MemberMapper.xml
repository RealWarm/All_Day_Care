<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.cen.mapper.BoardMapper">

	<select id="create"
			resultType="com.cen.domain.BoardVO">
		<!-- insert into tbl_board(bno, title, content, writer)
		values (seq_board.nextval, '테스트 제목', '테스트 내용', 'user00'); -->
	</select>
	
	<select id="count"
			resultType="string">
		<!-- select
		    count(*)
		from
		    tbl_board -->
		select
			sysdate
		from
			dual
	</select>

</mapper>